# Handoff — 2026-05-25

Краткий отчёт по PR #25: code review + CI/CD улучшения.

## 1. О чём работа

5-агентный code review (документация, программист, тестировщик, безопасность, тех-директор) с автоматическим исправлением найденного. Дополнительно: rebase на main с разрешением конфликтов.

## 2. Что сделано (PR #25)

### CI/CD
- Исправлена YAML-синтаксическая ошибка в `deploy.yml` (`: ` в plain scalar)
- Добавлен автоматический rollback при падении `Verify deployment` / `Smoke test` — восстанавливает из `.bak` через rsync; правильно различает отсутствие `.bak` (WARN) и сбой rsync (exit 1)
- `build.yml` (из main #23): вынесен reusable workflow, расширена валидация (privacy, legal, canonical, metrika.js)

### Приватность
- `nuxt.config.ts`: убран CSP с `unsafe-inline + https:` wildcard (main #24 добавил нормальный CSP без `unsafe-inline` в `script-src`; Метрика вынесена в `/metrika.js`)
- `webvisor:false` — Яндекс Метрика не записывает ввод в формах
- Адреса (vCard, реквизиты, `/legal`): убран номер квартиры

### Качество кода
- `BusinessCardModal`: `siteUrl` через `useRuntimeConfig()`, `import.meta.client` guard в `watch`, убраны явные Vue-импорты
- `app.vue`: убран `<ClientOnly>` с кнопки Telegram (консистентность с GitHub-кнопкой)
- `index.vue`: `id`-поля в интерфейсах, `:key` по id в `v-for`
- `SiteFooter`: `phoneTel` в объекте, убран `replace()` из шаблона
- `legal.vue`: `v-for :key` по лейблу вместо индекса
- `eslint.config.mjs`: `@typescript-eslint/no-explicit-any: error`
- `docs/copy-v5.md` → `legacy/`

### Инфраструктура
- `CLAUDE.md` создан в корне проекта (контекст для Claude Code сессий)

## 3. Что осталось открытым

- **Issue #4** (UptimeRobot): принято решение не добавлять в коде
- **Тесты**: Vitest/E2E не добавлялись — осознанное решение

## 4. Состояние main после мержа

Workflows:
- `ci.yml` — запускается на PR в main (lint + typecheck + generate)
- `build.yml` — reusable workflow (build + extended validate)
- `deploy.yml` — push в main → build.yml + rsync на VPS + rollback

Ключевые ENV переменные без изменений (см. README).
